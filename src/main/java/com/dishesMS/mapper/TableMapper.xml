<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dishesMS.dao.ITableDAO">
    
    <select id="selectTableByAN" resultType="com.dishesMS.model.Table">
        SELECT * from table_info where area_name like #{areaName} and number like #{number}
    </select>

    <select id="selectTable" resultType="com.dishesMS.model.Table">
        SELECT  * from table_info
    </select>

    <select id="selectTableById" resultType="com.dishesMS.model.Table">
        SELECT * from table_info where id like #{id}
    </select>

    <insert id="insertTable" parameterType="com.dishesMS.model.Table" keyProperty="id" useGeneratedKeys="true">
        insert into table_info(area_name,number,people_num,describe)
        VALUES (#{areaName},#{number},#{peopleNum},#{descibe})
    </insert>

    <update id="updateTable" parameterType="com.dishesMS.model.Table">
        UPDATE table_info SET  areaName = #{table.areaName},number = #{table.number},people_num = #{table.peopleNum},
        status = #{table.status},describe = #{table.describe} where id like #{table.id}
    </update>

    <delete id="deleteTable" parameterType="int">
        delete * from table_info where id like #{id}
    </delete>


</mapper>